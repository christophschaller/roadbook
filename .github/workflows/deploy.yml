name: Deploy to GitHub Pages

on:
  push:
    branches: [main]
    branches:
      - main
      - dev
  pull_request:
    branches:
      - '**'
  workflow_dispatch:

permissions:
  contents: read
  pages: write
@@ -20,14 +21,28 @@ jobs:
    steps:
      - name: Checkout your repository using git
        uses: actions/checkout@v4

      - name: Set base path environment variable
        run: |
          # If this is a pull_request event, use GITHUB_HEAD_REF; otherwise, use the pushed branch name.
          if [ -n "$GITHUB_HEAD_REF" ]; then
            branch="$GITHUB_HEAD_REF"
          else
            branch=${GITHUB_REF#refs/heads/}
          fi
          echo "Detected branch: $branch"
          if [ "$branch" = "main" ]; then
            echo "GITHUB_BASE_PATH=/roadbook" >> $GITHUB_ENV
          elif [ "$branch" = "dev" ]; then
            echo "GITHUB_BASE_PATH=/roadbook/dev" >> $GITHUB_ENV
          else
            echo "GITHUB_BASE_PATH=/roadbook/$branch" >> $GITHUB_ENV
          fi
      - name: Install, build, and upload your site
        uses: withastro/action@v3
        env:
          GITHUB_BASE_PATH: ${{ env.GITHUB_BASE_PATH }}

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4